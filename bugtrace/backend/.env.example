# BugTrace backend 环境变量模板
# 复制为 .env 并修改实际账号密码

# 本机开发：host=localhost（docker compose 已映射端口）
# 容器内运行：host=postgres
DATABASE_URL="postgresql://user:password@localhost:5432/dbname?schema=public"

# JWT 签名密钥（HS256，token 有效期 8h，T0-5）
JWT_SECRET="change-me-to-a-random-string"
